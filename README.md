# Prism V2

Replace the files in your existing GitHub repository with this version.

Your repository should contain:

```text
app/
  globals.css
  layout.tsx
  page.tsx
package.json
tsconfig.json
next-env.d.ts
README.md
```

After committing the changes, Vercel should deploy automatically to planwithprism.com.
