# Prism V3 — Full Sample Report

This build adds a complete sample report at:

`/report`

## Upload to GitHub

Replace the current files with these:

```text
app/
  globals.css
  layout.tsx
  page.tsx
  report/
    page.tsx
package.json
tsconfig.json
next-env.d.ts
README.md
```

After committing, Vercel will deploy automatically.

The homepage links to the sample report, and the sample report links back to the upload section.
